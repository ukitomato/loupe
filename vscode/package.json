{
  "name": "loupe-search",
  "displayName": "Loupe Search",
  "description": "Fast indexed full-text code search. A Rust (Tantivy) binary keeps a compact trigram index, decoding UTF-8, Shift_JIS and EUC-JP per folder, and updates incrementally as files change. Substring & regex, native, no Docker. Shines on large or multi-encoding codebases (e.g. a monorepo of many repos). One index is shared with the loupe CLI and MCP server.",
  "version": "0.4.0",
  "publisher": "ukitomato",
  "license": "MIT",
  "engines": { "vscode": "^1.75.0" },
  "categories": ["Other"],
  "keywords": ["search", "index", "incremental", "full-text", "regex", "trigram", "encoding", "shift_jis", "monorepo"],
  "repository": { "type": "git", "url": "https://github.com/ukitomato/loupe.git" },
  "main": "./out/extension.js",
  "activationEvents": ["onStartupFinished"],
  "contributes": {
    "commands": [
      { "command": "loupe.search", "title": "Search (substring)", "category": "loupe" },
      { "command": "loupe.searchRegex", "title": "Search (regex)", "category": "loupe" },
      { "command": "loupe.reindex", "title": "Build / rebuild index", "category": "loupe" },
      { "command": "loupe.focusSearch", "title": "Focus Search View", "category": "loupe" }
    ],
    "keybindings": [
      { "command": "loupe.search", "key": "ctrl+alt+f" },
      { "command": "loupe.focusSearch", "key": "ctrl+alt+shift+f" }
    ],
    "viewsContainers": {
      "activitybar": [
        { "id": "loupe", "title": "loupe", "icon": "images/icon.svg" }
      ]
    },
    "views": {
      "loupe": [
        { "id": "loupe.searchView", "type": "webview", "name": "Search" }
      ]
    },
    "configuration": {
      "title": "loupe",
      "properties": {
        "loupe.indexDir": {
          "type": "string", "default": "",
          "description": "Index directory, shared with the loupe CLI and MCP server. Empty = <workspace>/.loupe. Which folders are indexed (and their encodings, e.g. shift_jis) is configured in <indexDir>/settings.json — the single source of truth shared by all three front-ends — not in VS Code settings."
        },
        "loupe.binaryPath": {
          "type": "string", "default": "",
          "description": "Path to the loupe executable. Empty = bundled binary for this platform."
        },
        "loupe.maxResults": {
          "type": "integer", "default": 300,
          "description": "Maximum number of results shown."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && vsce package --allow-missing-repository",
    "pretest": "tsc -p tsconfig.test.json --noEmit && node esbuild.js && node esbuild.test.js",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.75.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.0",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.24.0",
    "mocha": "^10.0.0",
    "typescript": "^5.5.0"
  }
}
